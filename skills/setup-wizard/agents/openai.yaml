version: 1
interface:
  display_name: Setup Wizard
  short_description: Scan a repo and generate repo-specific Codex SDLC docs.
  default_prompt: Run the Codex SDLC setup wizard for this repo. Scan the project, infer the test harness and commands, ask only about unresolved preferences, then generate or update the repo-specific SDLC docs.
